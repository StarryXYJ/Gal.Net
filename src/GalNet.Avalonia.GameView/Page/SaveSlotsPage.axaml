<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:GalNet.Game.Controls"
             xmlns:local="using:GalNet.Avalonia.GameView.Page"
             xmlns:vm="using:GalNet.Avalonia.GameView.ViewModels"
             xmlns:services="using:GalNet.Avalonia.GameView.Services"
             x:Class="GalNet.Avalonia.GameView.Page.SaveSlotsPage"
             x:DataType="vm:SaveSlotsPageViewModel"
             x:CompileBindings="False">
  <Grid Margin="64" RowDefinitions="Auto,*,Auto" RowSpacing="14">
    <TextBlock Text="Save / Load" FontSize="28" FontWeight="SemiBold" />
    <ScrollViewer Grid.Row="1" MaxHeight="560">
      <ItemsControl ItemsSource="{Binding SaveSlots}">
        <ItemsControl.ItemTemplate>
          <DataTemplate DataType="services:GameSaveSlot">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8" Margin="0,4">
              <controls:SaveSlotCard SlotIndex="{Binding SlotIndex}" Timestamp="{Binding Timestamp}" Description="{Binding Description}" IsEmpty="{Binding IsEmpty}" IsCorrupt="{Binding IsCorrupt}"
                                      Command="{Binding DataContext.LoadSlotCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding SlotIndex}" />
              <Button Grid.Column="1" Content="Save" Command="{Binding DataContext.SaveSlotCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding SlotIndex}" VerticalAlignment="Center" />
            </Grid>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>
    </ScrollViewer>
    <Button Grid.Row="2" Content="Back" Command="{Binding BackCommand}" HorizontalAlignment="Right" />
  </Grid>
</UserControl>
