# 国际化

## 两条边界

编辑器 UI 与游戏文本使用不同的边界。

- 编辑器 UI 使用 `IEditorLocalizationService`。它通过 DynamicLocalization 从 `GalNet.Editor/Localization` 的嵌入 JSON 资源加载 `zh-CN`、`en-US`、`ja-JP`、`ko-KR`，切换语言时通知 `CurrentCulture` 与索引器绑定。
- Runtime 不依赖 DynamicLocalization 或某种 JSON 结构。宿主向 `GameEngine` 提供 `ITextResolver`，`EntryContext.GetText()` 用它解析文本；未提供时使用 `PassthroughTextResolver`，即把输入原样返回。

因此剧情文本字段（例如 `.galgroup` 内的 `speaker`、`content`，以及图中的分支选项）既可以直接保存文本，也可以保存宿主能识别的键。Runtime 不会自动生成、扫描或覆写游戏翻译文件。

## 键与稳定性

`I18nKey` 是点分隔键的轻量封装。推荐使用稳定的 Group/Branch ID，而不是可变的显示名称：

```csharp
I18nKey.ForEntry(groupId, entryId, "content");
I18nKey.ForBranchOption(branchId, optionIndex, "text");
```

这会生成如 `group-1.entry_3.content` 和 `branch-1.option_0.text` 的键。键的命名、翻译文件位置、回退策略及玩家覆盖方式均由具体宿主决定；它们不属于当前 Core/Runtime 文件格式的固定规范。

## 项目设置

`ProjectSettings` 保存目标语言 `TargetLocale` 与可用语言 `AvailableLocales`；`I18nLocale` 内置常用的 `zh-CN`、`zh-TW`、`en-US`、`ja-JP`、`ko-KR` 值。创建项目时会建立 `I18n/` 目录，并在导出时将其打入 `Assets/content.pak`，但当前导出器不解释该目录的内容。

资源的区域变体可由 `AssetLocaleResolver` 通过宿主选择的目录规则定位；这与文本的 `ITextResolver` 相互独立。
